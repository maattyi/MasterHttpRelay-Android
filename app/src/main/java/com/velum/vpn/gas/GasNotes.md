# Apps Script integration

The Android RelayClient speaks the same wire protocol as the desktop
DomainFronter — the canonical implementation of `Code.gs` lives in
`windows-app/app/gas/Code.gs`. Deploy that file as the GAS web app; it
works for both clients with no changes.
